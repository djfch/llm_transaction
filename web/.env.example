# 前端环境变量示例（复制为 .env 或 .env.development 生效）
#
# VITE_USE_MOCK：是否使用内置 mock 数据（后端未就绪时可独立开发预览）
#   true  —— 使用 src/api/mock.ts 假数据（默认值，未设置时等同 true）
#   false —— 走真实后端 /api 接口（vite dev server 已配置 proxy 到 127.0.0.1:8080）
VITE_USE_MOCK=true

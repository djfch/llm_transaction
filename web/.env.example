# 前端环境变量示例（复制为 .env 或 .env.development 生效）
#
# VITE_USE_MOCK：是否使用内置 mock 数据
#   false —— 走真实后端 /api 接口（默认值；vite dev server 已配置 proxy 到 127.0.0.1:17577）
#   true  —— 使用 src/api/mock.ts 假数据（仅后端未就绪时独立预览用；
#            注意：mock 下所有修改只存在于浏览器内存，刷新即复原）
VITE_USE_MOCK=false
